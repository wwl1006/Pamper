<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.work.pamper.mapper.PostMapper">

    <insert id="createPost" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post (user_id, title, content, images, video, post_type,
                         category, status, like_count, comment_count, view_count,
                         create_time, update_time)
        VALUES (#{user_id}, #{title}, #{content}, #{images}, #{video}, #{post_type},
                #{category}, #{status}, #{like_count}, #{comment_count}, #{view_count},
                #{create_time}, #{update_time})
    </insert>

    <select id="getPostById" resultType="com.work.pamper.entity.Post">
        SELECT p.*, a.username, a.avatar
        FROM post p
        LEFT JOIN account a ON p.user_id = a.id
        WHERE p.id = #{id}
    </select>

    <select id="getPostsWithPagination" resultType="com.work.pamper.entity.Post">
        SELECT p.*, a.username, a.avatar
        FROM post p
        LEFT JOIN account a ON p.user_id = a.id
        WHERE 1=1
        <if test="category != null and category != ''">
            AND p.category = #{category}
        </if>
        <if test="status != null">
            AND p.status = #{status}
        </if>
        ORDER BY p.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPosts" resultType="java.lang.Long">
        SELECT COUNT(*) FROM post
        WHERE 1=1
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <select id="getPostsByUserId" resultType="com.work.pamper.entity.Post">
        SELECT p.*, a.username, a.avatar
        FROM post p
        LEFT JOIN account a ON p.user_id = a.id
        WHERE p.user_id = #{userId}
        ORDER BY p.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <update id="updatePostStatus">
        UPDATE post SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="incrementViewCount">
        UPDATE post SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <update id="updateLikeCount">
        UPDATE post SET like_count = like_count + #{increment} WHERE id = #{id}
    </update>

    <update id="updateCommentCount">
        UPDATE post SET comment_count = comment_count + #{increment} WHERE id = #{id}
    </update>

    <delete id="deletePost">
        DELETE FROM post WHERE id = #{id}
    </delete>
</mapper>
